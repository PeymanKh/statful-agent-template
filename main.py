from src.agents.agent import Agent
from src.chains.chain import build_chain
from src.graphs.graph import build_graph

if __name__ == "__main__":
    # Example usage
    agent = Agent()
    chain = build_chain()
    graph = build_graph()
    print("Project initialized.")
